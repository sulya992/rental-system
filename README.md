# Real Estate Tinder

Monorepo for:
- backend (FastAPI + PostgreSQL)
- telegram-bot (future)
- web frontend (future)

## Quick start (local)

```bash
cp .env.example .env
docker compose build
docker compose up
